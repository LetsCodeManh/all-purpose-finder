# results — <slug>

Run YYYY-MM-DD · 0 new · 0 changed · 0 unchanged · 0 gone · 0 dropped at scoring

Read: <how deep — feed fields only, or bodies fetched>. Undecidable on that
alone: <criteria>.

What this run found, rewritten each run. Nothing is ticked here — the decision
lives in `shortlist.md` when `selection: rows`; `tools/shortlist.py` generates it
from this file. For `selection: artifact`, this whole file reaches GATE 4.

## new

### <issuer> — <item>
<!-- identity: <canonical value from criteria.md identity> -->

## changed

## unchanged

<issuer> — <item> · <score> <!-- identity: <canonical value> -->

## gone

## dropped at scoring

0 rows survived the prefilter and were then dropped by a `must` miss. Each one
names the criterion, so you can overrule it.

- **<issuer> — <item>** · <source> · [<link>](<url>) — must #<n> — <what it said>

The prefilter's own drops are not here. They are in `listings.md`, held as
`kept: no` rows with their reason.

## gaps this run

```
blocked, open by hand: <names>
manually checked:      <name> (<checked|partial|unavailable>, <date>)
page not read:         <name> — <why>
failed this run:       <name> (<error>)
not checked since:     <name> — <date>
```
